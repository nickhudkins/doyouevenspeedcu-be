steps:
  - id: "Everything"
    name: "gcr.io/cloud-builders/docker"
    args:
      ["build", "-t", "gcr.io/$PROJECT_ID/$REPO_NAME:$BRANCH_NAME-latest", "."]
images:
  - "gcr.io/$PROJECT_ID/$REPO_NAME:$BRANCH_NAME-latest"
